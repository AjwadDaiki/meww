{
  "name": "meowreel",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:infra": "docker compose -f docker-compose.dev.yml up -d",
    "dev:infra:down": "docker compose -f docker-compose.dev.yml down",
    "seed:categories": "pnpm --filter @meowreel/web exec tsx ../../scripts/seed-categories.ts",
    "seed:decors": "pnpm --filter @meowreel/web exec tsx ../../scripts/seed-decors.ts",
    "generate:assets": "pnpm --filter @meowreel/web exec tsx ../../scripts/generate-assets.ts"
  },
  "devDependencies": {
    "playwright": "^1.59.1",
    "turbo": "^2"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  }
}
